<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Confirm and Pay · AirbnbLite</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-white text-[#222222] font-sans antialiased">

  <%- include('partials/navbar') %>

  <main class="max-w-[1200px] mx-auto px-6 md:px-10 lg:px-20 pt-8 pb-20">
    
    <div class="flex items-center gap-4 mb-8">
      <a href="/homes/<%= home.id %>" class="p-2 hover:bg-gray-100 rounded-full transition">
        <svg viewBox="0 0 32 32" class="w-4 h-4" fill="none" stroke="currentColor" stroke-width="3"><path d="M20 28L8 16 20 4"></path></svg>
      </a>
      <h1 class="text-3xl font-semibold tracking-tight">Confirm and pay</h1>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-2 gap-16">
      
      <section>
        <h2 class="text-2xl font-semibold mb-6">Your trip</h2>
        
        <form action="/bookings/confirm" method="POST" class="space-y-8">
          <input type="hidden" name="homeId" value="<%= home.id %>">
          
          <div class="flex justify-between items-start">
            <div>
              <p class="font-semibold">Dates</p>
              <p class="text-gray-600 font-light">May 12 – 17, 2025</p>
            </div>
            <button type="button" class="underline font-semibold text-sm">Edit</button>
          </div>

          <div class="flex justify-between items-start">
            <div>
              <p class="font-semibold">Guests</p>
              <p class="text-gray-600 font-light">1 guest</p>
            </div>
            <button type="button" class="underline font-semibold text-sm">Edit</button>
          </div>

          <hr class="border-gray-200">

          <h2 class="text-2xl font-semibold mb-4">Pay with</h2>
          <div class="border border-gray-400 rounded-xl p-4 flex justify-between items-center cursor-pointer">
            <span class="text-gray-500 font-light italic">Credit or debit card...</span>
            <div class="flex gap-1">
                <img src="https://upload.wikimedia.org/wikipedia/commons/5/5e/Visa_Inc._logo.svg" class="h-3">
                <img src="https://upload.wikimedia.org/wikipedia/commons/2/2a/Mastercard-logo.svg" class="h-3">
            </div>
          </div>

          <hr class="border-gray-200">

          <div>
            <h3 class="text-xl font-semibold mb-2">Cancellation policy</h3>
            <p class="text-gray-600 font-light leading-relaxed">
              Free cancellation before May 5. After that, the reservation is non-refundable.
            </p>
          </div>

          <button type="submit" class="w-full md:w-auto bg-[#FF385C] text-white px-10 py-4 rounded-lg font-bold text-lg hover:bg-[#E31C5F] transition-all active:scale-95">
            Confirm Booking
          </button>
        </form>
      </section>

      <aside>
        <div class="sticky top-28 border border-gray-200 rounded-2xl p-6 shadow-sm">
          <div class="flex gap-4 mb-6">
            <img src="<%= home.image_url %>" class="w-28 h-20 object-cover rounded-lg" alt="Home">
            <div class="flex flex-col justify-between py-1">
              <div>
                <p class="text-xs text-gray-500"><%= home.location %></p>
                <p class="text-sm font-medium line-clamp-2"><%= home.title %></p>
              </div>
              <p class="text-xs font-semibold">★ 4.92 (128 reviews)</p>
            </div>
          </div>

          <hr class="border-gray-200 mb-6">

          <h3 class="text-xl font-semibold mb-4">Price details</h3>
          <div class="space-y-3 font-light text-[#222222]">
            <div class="flex justify-between">
              <span class="underline">₹<%= home.price.toLocaleString('en-IN') %> x 5 nights</span>
              <span>₹<%= (home.price * 5).toLocaleString('en-IN') %></span>
            </div>
            <div class="flex justify-between">
              <span class="underline">Airbnb service fee</span>
              <span>₹<%= Math.round(home.price * 0.15).toLocaleString('en-IN') %></span>
            </div>
            <div class="flex justify-between">
              <span class="underline">Taxes</span>
              <span>₹<%= Math.round(home.price * 0.08).toLocaleString('en-IN') %></span>
            </div>
          </div>

          <hr class="border-gray-200 my-6">

          <div class="flex justify-between font-bold text-lg">
            <span>Total (INR)</span>
            <span>₹<%= Math.round((home.price * 5) + (home.price * 0.23)).toLocaleString('en-IN') %></span>
          </div>
        </div>
      </aside>

    </div>
  </main>

  <%- include('partials/footer') %>

</body>
</html>