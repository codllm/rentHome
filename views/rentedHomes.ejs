<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Bookings · AirbnbLite</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <style>
    body { font-family: 'Inter', sans-serif; }
    
    /* Premium Glass Status Badges */
    .status-confirmed { background-color: rgba(240, 253, 244, 0.9); color: #166534; border: 1px solid rgba(187, 247, 208, 0.5); backdrop-filter: blur(4px); }
    .status-pending { background-color: rgba(255, 251, 235, 0.9); color: #92400e; border: 1px solid rgba(254, 243, 199, 0.5); backdrop-filter: blur(4px); }
    .status-cancelled { background-color: rgba(254, 242, 242, 0.9); color: #991b1b; border: 1px solid rgba(254, 226, 226, 0.5); backdrop-filter: blur(4px); }

    .no-scrollbar::-webkit-scrollbar { display: none; }
    
    /* Smooth Lift & Image Scale Effect */
    .booking-card { transition: transform 0.3s cubic-bezier(0.34, 1.56, 0.64, 1), box-shadow 0.3s ease; }
    .booking-card:hover { transform: translateY(-4px); }
    .booking-card:hover .property-img { transform: scale(1.1); }
  </style>
</head>
<body class="bg-[#fcfcfc] text-[#222222] antialiased">

  <%- include('partials/navbar') %>

  <main class="max-w-[1280px] mx-auto px-6 md:px-12 lg:px-20 py-16">
    
    <header class="mb-12 flex flex-col md:flex-row md:items-end md:justify-between gap-4">
      <div>
        <h1 class="text-4xl font-extrabold tracking-tight text-gray-900">Trips</h1>
        <p class="text-gray-500 mt-2 text-lg font-light">Your upcoming adventures and past memories.</p>
      </div>
      <div class="flex items-center gap-2 text-sm font-medium text-gray-600 bg-gray-100 px-4 py-2 rounded-full">
        <span class="w-2 h-2 bg-green-500 rounded-full animate-pulse"></span>
        <%= bookings.length %> total reservations
      </div>
    </header>

    <nav class="flex gap-10 border-b border-gray-100 mb-12 overflow-x-auto no-scrollbar scroll-smooth">
      <button class="pb-5 border-b-2 border-black font-bold text-sm whitespace-nowrap transition-all">All Bookings</button>
      <button class="pb-5 text-gray-400 hover:text-black transition-all text-sm whitespace-nowrap">Upcoming</button>
      <button class="pb-5 text-gray-400 hover:text-black transition-all text-sm whitespace-nowrap">Past trips</button>
      <button class="pb-5 text-gray-400 hover:text-black transition-all text-sm whitespace-nowrap">Cancelled</button>
    </nav>

    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-x-8 gap-y-16">
      
      <% if (bookings && bookings.length > 0) { %>
        <% bookings.forEach(booking => { %>
          <div class="booking-card group relative flex flex-col h-full bg-white rounded-3xl p-3 shadow-[0_2px_8px_rgba(0,0,0,0.04)] hover:shadow-[0_12px_24px_rgba(0,0,0,0.08)]">

            <div class="relative aspect-[4/3] rounded-2xl overflow-hidden bg-gray-100">
              <img src="<%= booking.image_url %>" 
                   alt="<%= booking.title %>"
                   class="property-img w-full h-full object-cover transition-transform duration-1000 ease-out"
                   onerror="this.src='https://images.unsplash.com/photo-1512917774080-9991f1c4c750?auto=format&fit=crop&w=800'">
              
              <div class="absolute top-4 left-4 z-10">
                <span class="px-3 py-1.5 rounded-xl text-[10px] font-black uppercase tracking-widest shadow-sm 
                  <%= booking.status.toLowerCase() === 'confirmed' ? 'status-confirmed' : booking.status.toLowerCase() === 'pending' ? 'status-pending' : 'status-cancelled' %>">
                  <%= booking.status %>
                </span>
              </div>

              <button class="absolute top-4 right-4 z-10 p-2 bg-white/20 backdrop-blur-md rounded-full hover:bg-white transition-all group/fav">
                <svg viewBox="0 0 32 32" class="w-5 h-5 fill-white group-hover/fav:fill-[#FF385C] transition-colors"><path d="M16 28c7-4.73 14-10 14-17a6.98 6.98 0 0 0-7-7c-1.8 0-3.58.68-4.95 2.05L16 8.1l-2.05-2.05a6.98 6.98 0 0 0-9.9 0 6.98 6.98 0 0 0 0 9.9z"></path></svg>
              </button>
            </div>

            <div class="px-3 pt-5 pb-2 flex flex-col flex-grow">
              <div class="flex justify-between items-start mb-1">
                <h3 class="font-bold text-[17px] text-[#222222] truncate max-w-[80%]"><%= booking.title %></h3>
                <div class="flex items-center gap-1">
                  <span class="text-xs">★</span>
                  <span class="text-sm font-medium">4.98</span>
                </div>
              </div>
              
              <p class="text-gray-500 text-[14px] font-light truncate"><%= booking.location %></p>
              <p class="text-gray-400 text-[13px] font-light mt-1 flex items-center gap-2">
                <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" /></svg>
                Booked <%= new Date(booking.created_at).toLocaleDateString('en-GB', { day: 'numeric', month: 'short' }) %>
              </p>
              
              <div class="mt-6 pt-5 border-t border-gray-50 flex items-center justify-between">
                <div>
                    <span class="text-[16px] font-extrabold text-black">₹<%= Number(booking.amount).toLocaleString('en-IN') %></span>
                    <span class="text-xs text-gray-400 font-light ml-1 uppercase tracking-tighter">Paid Total</span>
                </div>
              </div>
              
              <div class="grid grid-cols-2 gap-3 mt-4">
                <a href="/homes/<%= booking.id %>" class="flex items-center justify-center py-2.5 border border-gray-200 rounded-xl text-xs font-bold uppercase tracking-wider text-gray-600 hover:bg-gray-50 hover:text-black hover:border-black transition-all">
                  Details
                </a>
                
                <% if (booking.status.toLowerCase() === 'confirmed') { %>
                  <a href="/receipt/booking/<%= booking.bookingId %>/home/<%= booking.homeId %>" class="flex items-center justify-center px-4 py-2.5 bg-[#222222] text-white rounded-xl text-xs font-bold uppercase tracking-wider hover:bg-black hover:shadow-lg active:scale-[0.96] transition-all">
                    <svg class="w-3.5 h-3.5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
                    </svg>
                    Receipt
                  </a>
                <% } else { %>
                    <button disabled class="flex items-center justify-center py-2.5 bg-gray-50 text-gray-300 rounded-xl text-xs font-bold uppercase tracking-wider cursor-not-allowed">
                        Receipt
                    </button>
                <% } %>
              </div>
            </div>
          </div>
        <% }) %>
      <% } else { %>
        <div class="col-span-full py-32 flex flex-col items-center justify-center text-center">
          <div class="relative mb-8">
            <div class="w-24 h-24 bg-gray-50 rounded-full flex items-center justify-center">
                <svg viewBox="0 0 32 32" class="w-10 h-10 text-gray-200"><path d="M24 5H8a3 3 0 0 0-3 3v16a3 3 0 0 0 3 3h16a3 3 0 0 0 3-3V8a3 3 0 0 0-3-3zm1 19a1 1 0 0 1-1 1H8a1 1 0 0 1-1-1V8a1 1 0 0 1 1-1h16a1 1 0 0 1 1 1zM11 11h10v2H11zm0 4h10v2H11zm0 4h7v2H11z"></path></svg>
            </div>
          </div>
          <h2 class="text-3xl font-bold text-gray-900">No trips booked yet</h2>
          <p class="text-gray-500 mt-3 max-w-sm text-lg font-light leading-relaxed">Time to dust off your bags and start planning your next big adventure.</p>
          <a href="/" class="mt-10 px-12 py-4 bg-[#FF385C] text-white font-bold rounded-2xl shadow-[0_8px_20px_rgba(255,56,92,0.3)] hover:shadow-[0_12px_24px_rgba(255,56,92,0.4)] hover:-translate-y-1 transition-all active:scale-95">
            Start exploring
          </a>
        </div>
      <% } %>

    </div>
  </main>

  <%- include('partials/footer') %>

</body>
</html>