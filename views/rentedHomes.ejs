<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Bookings · AirbnbLite</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <style>
    body { font-family: 'Inter', sans-serif; }
    
    /* Clean Airbnb-style Status Badges */
    .status-confirmed { background-color: #f0fdf4; color: #166534; border: 1px solid #bbf7d0; }
    .status-pending { background-color: #fffbeb; color: #92400e; border: 1px solid #fef3c7; }
    .status-cancelled { background-color: #fef2f2; color: #991b1b; border: 1px solid #fee2e2; }

    .no-scrollbar::-webkit-scrollbar { display: none; }
    
    /* Image Hover Effect */
    .booking-card:hover .property-img { transform: scale(1.08); }
    .booking-card { transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1); }
  </style>
</head>
<body class="bg-white text-[#222222] antialiased">

  <%- include('partials/navbar') %>

  <main class="max-w-[1280px] mx-auto px-6 md:px-12 lg:px-20 py-12">
    
    <header class="mb-12">
      <h1 class="text-4xl font-extrabold tracking-tight mb-2">Trips</h1>
      <p class="text-lg text-gray-500 font-light">Upcoming and past stays at your favorite places.</p>
    </header>

    <nav class="flex gap-8 border-b border-gray-100 mb-10 overflow-x-auto no-scrollbar scroll-smooth">
      <button class="pb-5 border-b-2 border-black font-bold text-sm whitespace-nowrap">All Bookings</button>
      <button class="pb-5 text-gray-400 hover:text-black hover:border-b-2 hover:border-gray-200 transition-all text-sm whitespace-nowrap">Upcoming</button>
      <button class="pb-5 text-gray-400 hover:text-black hover:border-b-2 hover:border-gray-200 transition-all text-sm whitespace-nowrap">Past trips</button>
      <button class="pb-5 text-gray-400 hover:text-black hover:border-b-2 hover:border-gray-200 transition-all text-sm whitespace-nowrap">Cancelled</button>
    </nav>

    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-3 gap-x-8 gap-y-12">
      
      <% if (bookings && bookings.length > 0) { %>
        <% bookings.forEach(booking => { %>
          <div class="booking-card group relative flex flex-col h-full">

            
            
            <div class="relative aspect-[4/3] rounded-2xl overflow-hidden bg-gray-100 shadow-sm">
              <img src="<%= booking.image_url %>" 
                   alt="<%= booking.title %>"
                   class="property-img w-full h-full object-cover transition-transform duration-700 ease-out"
                   onerror="this.src='https://images.unsplash.com/photo-1512917774080-9991f1c4c750?q=80&w=1000&auto=format&fit=crop'">
              
              <div class="absolute top-4 left-4 z-10">
                <span class="px-3 py-1.5 rounded-lg text-[10px] font-black uppercase tracking-widest shadow-sm 
                  <%= booking.status.toLowerCase() === 'confirmed' ? 'status-confirmed' : booking.status.toLowerCase() === 'pending' ? 'status-pending' : 'status-cancelled' %>">
                  <%= booking.status %>
                </span>
              </div>

              <div class="absolute top-4 right-4 z-10">
                <svg viewBox="0 0 32 32" class="w-6 h-6 fill-black/20 stroke-white stroke-2 hover:fill-[#FF385C] transition-colors"><path d="M16 28c7-4.73 14-10 14-17a6.98 6.98 0 0 0-7-7c-1.8 0-3.58.68-4.95 2.05L16 8.1l-2.05-2.05a6.98 6.98 0 0 0-9.9 0 6.98 6.98 0 0 0 0 9.9z"></path></svg>
              </div>
            </div>

          

            <div class="pt-4 flex flex-col flex-grow">
              <div class="flex justify-between items-start mb-1">
                <h3 class="font-bold text-[16px] text-[#222222] truncate pr-2"><%= booking.title %></h3>
                <div class="flex items-center gap-1 text-sm">
                  <span class="text-xs">★</span>
                  <span class="font-light">4.9</span>
                </div>
              </div>
              
              <p class="text-gray-500 text-[14px] font-light mb-1"><%= booking.location %></p>
              <p class="text-gray-500 text-[14px] font-light mb-3">Booked on <%= new Date(booking.created_at).toLocaleDateString('en-GB', { day: 'numeric', month: 'short' }) %></p>
              
              <p class="text-[15px] font-bold mt-auto pb-4">
                ₹<%= Number(booking.amount).toLocaleString('en-IN') %> <span class="font-light text-gray-500">total</span>
              </p>
              
              <div class="grid grid-cols-2 gap-3">
                <a href="/homes/<%= booking.home_id %>" class="flex items-center justify-center py-2.5 border border-gray-300 rounded-lg text-sm font-semibold hover:bg-gray-50 hover:border-black transition-all">
                  Details
                </a>
                
                <% if (booking.status.toLowerCase() === 'confirmed') { %>
                  <button class="flex items-center justify-center py-2.5 bg-[#222222] text-white rounded-lg text-sm font-semibold hover:bg-black active:scale-95 transition-all">
                    Receipt
                  </button>
                <% } else { %>
                    <button disabled class="flex items-center justify-center py-2.5 bg-gray-100 text-gray-400 rounded-lg text-sm font-semibold cursor-not-allowed">
                        Invoice
                    </button>
                <% } %>
              </div>
            </div>
          </div>
        <% }) %>
      <% } else { %>
        <div class="col-span-full py-24 flex flex-col items-center justify-center text-center border-2 border-dashed border-gray-100 rounded-3xl">
          <div class="w-20 h-20 bg-gray-50 rounded-full flex items-center justify-center mb-6">
            <svg viewBox="0 0 32 32" class="w-10 h-10 text-gray-300"><path d="M24 5H8a3 3 0 0 0-3 3v16a3 3 0 0 0 3 3h16a3 3 0 0 0 3-3V8a3 3 0 0 0-3-3zm1 19a1 1 0 0 1-1 1H8a1 1 0 0 1-1-1V8a1 1 0 0 1 1-1h16a1 1 0 0 1 1 1zM11 11h10v2H11zm0 4h10v2H11zm0 4h7v2H11z"></path></svg>
          </div>
          <h2 class="text-2xl font-bold">No trips booked...yet!</h2>
          <p class="text-gray-500 mt-2 max-w-xs">When you're ready to plan your next trip, we're here to help.</p>
          <a href="/" class="mt-8 px-10 py-3.5 bg-[#FF385C] text-white font-bold rounded-xl hover:shadow-lg transition-all active:scale-95">
            Start exploring
          </a>
        </div>
      <% } %>

    </div>
  </main>

  <%- include('partials/footer') %>

</body>
</html>