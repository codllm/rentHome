<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Bookings · AirbnbLite</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <style>
    body { font-family: 'Inter', sans-serif; }

    /* Premium Glass Status Badges */
    .status-confirmed { background: rgba(34, 197, 94, 0.1); color: #15803d; border: 1px solid rgba(34, 197, 94, 0.2); }
    .status-pending { background: rgba(234, 179, 8, 0.1); color: #a16207; border: 1px solid rgba(234, 179, 8, 0.2); }
    .status-cancelled { background: rgba(239, 68, 68, 0.1); color: #b91c1c; border: 1px solid rgba(239, 68, 68, 0.2); }

    .no-scrollbar::-webkit-scrollbar { display: none; }

    .booking-card {
      transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    }

    .booking-card:hover {
      transform: translateY(-8px);
      box-shadow: 0 20px 25px -5px rgb(0 0 0 / 0.1), 0 8px 10px -6px rgb(0 0 0 / 0.1);
    }

    .image-zoom { transition: transform 0.8s cubic-bezier(0.4, 0, 0.2, 1); }
    .booking-card:hover .image-zoom { transform: scale(1.08); }
  </style>
</head>

<body class="bg-[#f9fafb] text-[#222222] antialiased">

  <%- include('partials/navbar') %>

  <main class="max-w-[1400px] mx-auto px-6 md:px-12 py-12">

    <header class="mb-12">
      <div class="flex flex-col md:flex-row md:items-center justify-between gap-6">
        <div>
          <h1 class="text-5xl font-extrabold text-gray-900 tracking-tight">Trips</h1>
          <p class="text-gray-500 mt-3 text-lg font-light italic">Your world, organized.</p>
        </div>
        <div class="inline-flex items-center gap-3 bg-white border border-gray-100 px-6 py-3 rounded-2xl shadow-sm">
          <div class="flex -space-x-2">
            <div class="w-8 h-8 rounded-full border-2 border-white bg-green-400"></div>
            <div class="w-8 h-8 rounded-full border-2 border-white bg-blue-400"></div>
          </div>
          <span class="text-sm font-semibold text-gray-700"><%= bookings.length %> Active Trips</span>
        </div>
      </div>
    </header>

    <nav class="sticky top-20 z-30 flex gap-8 border-b border-gray-200 mb-12 bg-[#f9fafb]/80 backdrop-blur-md no-scrollbar overflow-x-auto">
      <button class="pb-4 border-b-2 border-black font-bold text-sm tracking-wide">All stays</button>
      <button class="pb-4 text-gray-400 hover:text-gray-600 font-medium text-sm transition-all">Upcoming</button>
      <button class="pb-4 text-gray-400 hover:text-gray-600 font-medium text-sm transition-all">Past</button>
      <button class="pb-4 text-gray-400 hover:text-gray-600 font-medium text-sm transition-all">Cancelled</button>
    </nav>

    <div class="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-10">
      <% if (bookings && bookings.length > 0) { %>
        <% bookings.forEach(booking => { %>
          <div class="booking-card bg-white rounded-[2rem] overflow-hidden border border-gray-100 flex flex-col">
            
            <div class="relative h-64 overflow-hidden">
              <img src="<%= booking.image_url %>" 
                   class="image-zoom w-full h-full object-cover" 
                   alt="<%= booking.title %>">
              
              <div class="absolute top-5 left-5">
                <span class="px-4 py-1.5 rounded-full text-[11px] font-bold uppercase tracking-wider backdrop-blur-md shadow-lg
                  <%= booking.status.toLowerCase() === 'confirmed' ? 'status-confirmed' : booking.status.toLowerCase() === 'pending' ? 'status-pending' : 'status-cancelled' %>">
                  <%= booking.status %>
                </span>
              </div>

              <div class="absolute bottom-5 right-5 bg-white/90 backdrop-blur-sm px-4 py-2 rounded-2xl text-center shadow-lg border border-white/50">
                <span class="block text-[10px] font-black uppercase text-gray-400 leading-none">Nov</span>
                <span class="block text-xl font-bold text-gray-900 leading-tight">12</span>
              </div>
            </div>

            <div class="p-8 flex-grow">
              <div class="flex justify-between items-start mb-2">
                <h3 class="text-xl font-bold text-gray-900 leading-tight truncate"><%= booking.title %></h3>
                <div class="flex items-center text-sm font-bold bg-gray-50 px-2 py-1 rounded-lg">
                  <span class="text-orange-400 mr-1">★</span> 4.9
                </div>
              </div>
              
              <p class="text-gray-500 font-light text-sm flex items-center gap-1 mb-6">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/><path d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/></svg>
                <%= booking.location %>
              </p>

              <div class="flex items-center justify-between">
                <div>
                  <p class="text-[10px] font-bold text-gray-400 uppercase tracking-widest">Total Price</p>
                  <p class="text-2xl font-black text-gray-900">₹<%= Number(booking.amount).toLocaleString('en-IN') %></p>
                </div>

                <% if (booking.status.toLowerCase() === 'confirmed') { %>
                  <a href="/receipt/booking/<%= booking.bookingId %>/home/<%= booking.homeId %>" 
                     class="px-6 py-3 bg-[#222222] hover:bg-black text-white rounded-2xl text-xs font-bold transition-all active:scale-95 flex items-center gap-2">
                     <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" /></svg>
                     Receipt
                  </a>
                <% } else { %>
                  <button disabled class="px-6 py-3 bg-gray-100 text-gray-400 rounded-2xl text-xs font-bold cursor-not-allowed">
                    Receipt
                  </button>
                <% } %>
              </div>
            </div>

            <a href="/homes/<%= booking.homeId %>" class="block w-full text-center py-4 bg-gray-50 hover:bg-gray-100 text-gray-600 text-[11px] font-bold uppercase tracking-widest transition-colors border-t border-gray-100">
              View Property Details
            </a>

          </div>
        <% }) %>
      <% } else { %>
        <% } %>
    </div>
  </main>

  <%- include('partials/footer') %>

</body>
</html>