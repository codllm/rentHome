<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Manage Booking · <%= receiptData.title %></title>

  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">

  <style>
    body { font-family: 'Inter', sans-serif; }
    .cancel-gradient {
      background: linear-gradient(135deg, #fff5f5 0%, #ffffff 100%);
    }
  </style>
</head>

<body class="bg-gray-50 text-[#222222] antialiased pb-20">

  <%- include('partials/navbar') %>

  <main class="max-w-[1000px] mx-auto px-6 pt-12">

    <!-- ================= BOOKING CARD ================= -->
    <div class="bg-white rounded-3xl overflow-hidden shadow-sm border border-gray-100 mb-8">
      <div class="grid grid-cols-1 md:grid-cols-2">

        <div class="h-64 md:h-auto overflow-hidden">
          <img src="<%= receiptData.image_url %>" class="w-full h-full object-cover" />
        </div>

        <div class="p-8 flex flex-col justify-center">
          <div class="flex items-center gap-2 mb-2">
            <span class="bg-green-100 text-green-700 text-[10px] font-black px-2 py-0.5 rounded uppercase">
              <%= receiptData.status %>
            </span>

           
          </div>

          <h1 class="text-3xl font-bold leading-tight mb-2">
            <%= receiptData.title %>
          </h1>

          <p class="text-gray-500 mb-6">
            <%= receiptData.location %>
          </p>

          <div class="grid grid-cols-2 gap-4 py-4 border-y border-gray-50">
            <div>
              <p class="text-[10px] font-bold text-gray-400 uppercase">Check-in</p>
              <p class="font-semibold text-sm">
                <%= new Date(receiptData.checkin).toDateString() %>
              </p>
            </div>
            <div>
              <p class="text-[10px] font-bold text-gray-400 uppercase">Checkout</p>
              <p class="font-semibold text-sm">
                <%= new Date(receiptData.checkout).toDateString() %>
              </p>
            </div>
          </div>

          <div class="mt-4 text-sm font-semibold">
            Amount Paid: ₹<%= receiptData.amount %>
          </div>
        </div>
      </div>
    </div>

    <!-- ================= MAIN GRID ================= -->
    <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">

      <!-- ================= LEFT ================= -->
      <div class="lg:col-span-2 space-y-8">

        <!-- ================= CANCELLATION ================= -->
        <div class="cancel-gradient border border-red-100 rounded-3xl p-8 relative overflow-hidden">
          <div class="relative z-10">
            <h2 class="text-xl font-bold text-red-900 mb-2">Cancellation Options</h2>

            <%
              const bookingTime = new Date(receiptData.created_at).getTime();
              const currentTime = new Date().getTime();
              const hoursPassed = (currentTime - bookingTime) / (1000 * 60 * 60);
              const canCancel = hoursPassed <= 5;
            %>

            <% if (canCancel) { %>
              <p class="text-red-700 text-sm mb-6 leading-relaxed">
                You are within the <strong>5-hour grace period</strong>.
                Cancel now for a <strong>full refund</strong>.
              </p>

              <form action="/bookings/cancel/<%= receiptData.booking_id %>" method="POST">
                <button type="submit"
                  class="bg-red-600 text-white px-8 py-3 rounded-xl font-bold hover:bg-red-700 transition active:scale-95">
                  Cancel Reservation
                </button>
              </form>

            <% } else { %>
              <p class="text-gray-500 text-sm mb-4">
                Free cancellation window has expired.
              </p>

              <button disabled
                class="bg-gray-200 text-gray-400 px-8 py-3 rounded-xl font-bold cursor-not-allowed">
                Cancellation Locked
              </button>
            <% } %>
          </div>
        </div>

        <!-- ================= REVIEW ================= -->
        <div class="bg-white border border-gray-100 rounded-3xl p-8 shadow-sm">
          <h2 class="text-xl font-bold mb-6">Leave a review</h2>

          <form action="/reviews/add" method="POST" class="space-y-4">
            <input type="hidden" name="homeId" value="<%= receiptData.home_id %>" />
            <input type="hidden" name="bookingId" value="<%= receiptData.booking_id %>" />

            <div class="flex gap-2 mb-4">
              <% for (let i = 1; i <= 5; i++) { %>
                <span class="text-2xl text-yellow-400">★</span>
              <% } %>
            </div>

            <textarea
              name="comment"
              class="w-full h-32 p-4 bg-gray-50 border border-gray-100 rounded-2xl focus:ring-2 focus:ring-black outline-none resize-none text-sm"
              placeholder="How was your stay?"
            ></textarea>

            <button type="submit"
              class="w-full bg-[#222222] text-white py-4 rounded-xl font-bold hover:bg-black transition">
              Submit Review
            </button>
          </form>
        </div>
      </div>

      <!-- ================= RIGHT ================= -->
      <aside class="space-y-6">
        <div class="bg-white border border-gray-100 rounded-3xl p-6 shadow-sm">
          <h3 class="font-bold mb-4">Need help?</h3>
          <ul class="space-y-4 text-sm text-gray-600">
            <li>💬 Chat with Host</li>
            <li>🛡️ AirCover Protection</li>
            <li>📞 Contact Support</li>
          </ul>
        </div>

        <div class="p-6">
          <h3 class="font-bold mb-2 text-sm">House Rules</h3>
          <p class="text-xs text-gray-500 leading-relaxed">
            🚭 No smoking <br />
            🐾 No pets <br />
            🤫 Quiet hours: 10 PM – 7 AM
          </p>
        </div>
      </aside>

    </div>
  </main>

  <%- include('partials/footer') %>

</body>
</html>
