<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>Vacation Rentals, Homes & Experiences · AirbnbLite</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <script src="https://cdn.tailwindcss.com"></script>

  <style>
    .aspect-card { aspect-ratio: 20/19; }
    html { scroll-behavior: smooth; }
    
    /* Hide scrollbar for cleaner horizontal scrolling */
    .no-scrollbar::-webkit-scrollbar { display: none; }
    .no-scrollbar { 
      -ms-overflow-style: none;  /* IE and Edge */
      scrollbar-width: none;  /* Firefox */
    }

    .hero-gradient {
      background: linear-gradient(to bottom, rgba(0,0,0,0.3) 0%, rgba(0,0,0,0.1) 50%, rgba(0,0,0,0.4) 100%);
    }
  </style>
</head>

<body class="bg-white font-sans antialiased text-[#222222]">

  <%- include('partials/navbar') %>

  <div class="relative w-full h-[60vh] min-h-[400px] flex items-center justify-center overflow-hidden">
    <div class="absolute inset-0 z-0">
      <img src="https://images.unsplash.com/photo-1540541338287-41700207dee6?q=80&w=2500&auto=format&fit=crop" class="w-full h-full object-cover">
      <div class="absolute inset-0 hero-gradient"></div>
    </div>
    <div class="relative z-10 text-center px-6 max-w-4xl">
      <h1 class="text-5xl md:text-6xl font-bold text-white mb-4 drop-shadow-md">Find your perfect view.</h1>
      <a href="#available-homes" class="inline-block bg-[#FF385C] text-white px-10 py-4 rounded-full font-bold text-lg shadow-xl hover:scale-105 transition-transform">Start exploring</a>
    </div>
  </div>

  <main id="available-homes" class="max-w-[1600px] mx-auto px-6 md:px-10 lg:px-20 py-12">

    <% for (let state in stateWiseHomes) { %>
      <% if (stateWiseHomes[state].length > 0) { %>
    
        <div class="mb-12">
          <div class="flex items-center justify-between mb-6">
            <h2 class="text-2xl font-bold tracking-tight text-[#222222] capitalize">
              Popular in <%= state %>
            </h2>
            <div class="flex gap-2">
              <button class="p-2 border border-gray-200 rounded-full hover:shadow-md transition bg-white">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path d="M15 19l-7-7 7-7" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"></path></svg>
              </button>
              <button class="p-2 border border-gray-200 rounded-full hover:shadow-md transition bg-white">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path d="M9 5l7 7-7 7" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"></path></svg>
              </button>
            </div>
          </div>

          <div class="flex overflow-x-auto gap-5 no-scrollbar pb-4 snap-x snap-mandatory">

            <% stateWiseHomes[state].forEach(home => { %>

              <div class="min-w-[280px] md:min-w-[320px] group relative flex flex-col snap-start">

                <form action="/favourite-home/<%= home.id %>" method="post" class="absolute top-3 right-3 z-20">
                  <button type="submit" class="p-1 group/heart">
                    <svg viewBox="0 0 32 32" class="w-6 h-6 stroke-white stroke-[2px] fill-black/30 group-hover/heart:fill-[#FF385C] transition-all">
                      <path d="M16 28c7-4.73 14-10 14-17a6.98 6.98 0 0 0-7-7c-1.8 0-3.58.68-4.95 2.05L16 8.1l-2.05-2.05a6.98 6.98 0 0 0-9.9 0 6.98 6.98 0 0 0 0 9.9z"></path>
                    </svg>
                  </button>
                </form>

                <a href="/homes/<%= home.id %>" class="flex flex-col">
                  <div class="relative overflow-hidden rounded-xl aspect-card mb-3 bg-gray-100">
                    <img src="<%= home.image_url %>" class="h-full w-full object-cover group-hover:scale-105 transition-transform duration-500 ease-out" loading="lazy">
                    
                    <div class="absolute top-3 left-3 bg-white/95 px-2.5 py-1 rounded-md text-[11px] font-bold shadow-sm border border-gray-100 uppercase tracking-tight text-gray-800">
                      Guest Favourite
                    </div>
                  </div>

                  <div class="flex flex-col space-y-0.5">
                    <div class="flex justify-between items-start">
                      <h3 class="font-bold text-[15px] truncate text-[#222222]">
                        <%= home.location %>
                      </h3>
                      <div class="flex items-center gap-1 text-sm">
                        <span>★</span>
                        <span class="font-light">4.9</span>
                      </div>
                    </div>
                    <p class="text-[#717171] text-[14px] truncate"><%= home.title %></p>
                    <p class="text-[#717171] text-[14px]">May 12 – 17</p>
                    <p class="mt-1 text-[15px]">
                      <span class="font-bold text-[#222222]">₹<%= home.price.toLocaleString('en-IN') %></span> night
                    </p>
                  </div>
                </a>
              </div>

            <% }) %>
          </div>
        </div>

      <% } %>
    <% } %>

  </main>
  <%- include('partials/footer') %>
</body>
</html>