<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Wishlists · AirbnbLite</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    .aspect-card { aspect-ratio: 1 / 1; }
  </style>
</head>

<body class="bg-white text-[#222222] antialiased min-h-screen">

  <%- include('partials/navbar') %>

  <main class="max-w-[1600px] mx-auto px-6 md:px-10 lg:px-20 py-12">
    
    <header class="mb-10">
      <h1 class="text-3xl font-semibold tracking-tight">Your Favourites</h1>
      <p class="text-gray-500 mt-2"><%= favourites.length %> saved places</p>
    </header>

    <% if (favourites.length === 0) { %>
      <div class="mt-20 flex flex-col items-start max-w-md">
        <h2 class="text-2xl font-semibold mb-2">No favourites yet</h2>
        <p class="text-gray-500 mb-8">Click the heart icon on any home to save it here for later.</p>
        <a href="/" class="px-6 py-3 bg-black text-white font-semibold rounded-lg hover:bg-gray-800 transition">
          Browse Homes
        </a>
      </div>
    <% } else { %>

      <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 xl:grid-cols-5 gap-x-6 gap-y-12">

        <% for (let fab of favourites) { %>

          <div class="group flex flex-col">
            
            <div class="relative overflow-hidden rounded-2xl aspect-card mb-4 bg-gray-100 shadow-sm">
              <img 
                src="<%= fab.image_url %>" 
                class="h-full w-full object-cover transition-transform duration-500 group-hover:scale-105" 
                alt="<%= fab.title %>"
              >
              
              <form action="/remove-favourite/<%= fab.id %>" method="post" class="absolute top-3 right-3 z-10">
                <button type="submit" class="bg-white/80 backdrop-blur-sm p-2 rounded-full shadow-sm hover:bg-white transition">
                  <svg viewBox="0 0 32 32" class="w-5 h-5 fill-[#FF385C]">
                    <path d="M16 28c7-4.73 14-10 14-17a6.98 6.98 0 0 0-7-7c-1.8 0-3.58.68-4.95 2.05L16 8.1l-2.05-2.05a6.98 6.98 0 0 0-9.9 0 6.98 6.98 0 0 0 0 9.9z"></path>
                  </svg>
                </button>
              </form>
            </div>

            <div class="flex-1">
              <div class="flex justify-between items-start mb-1">
                <h3 class="font-bold text-[15px] truncate"><%= fab.location %></h3>
                <span class="text-sm">★ 4.9</span>
              </div>
              <p class="text-gray-500 text-sm truncate mb-1"><%= fab.title %></p>
              <p class="text-[15px] mb-4">
                <span class="font-bold">₹<%= fab.price %></span> <span class="text-gray-500">night</span>
              </p>

              <div class="flex gap-2">
                <a href="/homes/<%= fab.id %>" 
                   class="flex-[2] text-center bg-[#FF385C] text-white py-2.5 rounded-lg text-sm font-semibold hover:bg-[#E31C5F] transition active:scale-95 shadow-sm">
                  Book Now
                </a>

                <form action="/remove-favourite/<%= fab.id %>" method="post" class="flex-1">
                  <button type="submit" 
                          class="w-full h-full border border-gray-300 py-2.5 rounded-lg text-sm font-semibold hover:bg-gray-50 transition text-gray-700">
                    Remove
                  </button>
                </form>
              </div>
            </div>

          </div>

        <% } %>

      </div>
    <% } %>

  </main>

</body>
</html>