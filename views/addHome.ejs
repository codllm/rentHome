<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Host your home · AirbnbLite</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-white md:bg-gray-50 min-h-screen font-sans antialiased text-[#222222]">

  <%- include('partials/navbar') %>

  <div class="max-w-2xl mx-auto px-6 py-12">

    <div class="mb-10">
      <h1 class="text-3xl font-semibold tracking-tight mb-2">
        Add your home
      </h1>
      <p class="text-gray-500">Tell us about your place to start reaching guests.</p>
    </div>

    <div class="bg-white md:border md:border-gray-200 md:rounded-2xl md:shadow-xl p-6 md:p-10">
      
      <form class="space-y-6" method="post" action="/host/addhomeFormpage" enctype="multipart/form-data">

        <div class="relative">
          <label class="block text-[12px] font-bold uppercase tracking-wider text-gray-500 mb-1 ml-1">Title</label>
          <input
            type="text"
            name="title"
            placeholder="e.g. Cozy Beachfront Villa"
            required
            class="w-full border border-gray-300 rounded-xl px-4 py-4 text-lg focus:outline-none focus:ring-2 focus:ring-black focus:border-transparent transition-all placeholder:text-gray-300"
          />
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
          <div>
            <label class="block text-[12px] font-bold uppercase tracking-wider text-gray-500 mb-1 ml-1">Location</label>
            <input
              type="text"
              placeholder="Varanasi, India"
              name="location"
              required
              class="w-full border border-gray-300 rounded-xl px-4 py-3 focus:outline-none focus:ring-2 focus:ring-black transition-all placeholder:text-gray-300"
            />
          </div>

          <div>
            <label class="block text-[12px] font-bold uppercase tracking-wider text-gray-500 mb-1 ml-1">Price per night (₹)</label>
            <input
              type="number"
              placeholder="2500"
              name="price"
              required
              class="w-full border border-gray-300 rounded-xl px-4 py-3 focus:outline-none focus:ring-2 focus:ring-black transition-all placeholder:text-gray-300"
            />
          </div>

          <div>
            <label class="block text-[12px] font-bold uppercase tracking-wider text-gray-500 mb-1 ml-1">No of bedrooms</label>
            <input
              type="number"
              placeholder="2"
              name="bedroom"
              required
              class="w-full border border-gray-300 rounded-xl px-4 py-3 focus:outline-none focus:ring-2 focus:ring-black transition-all placeholder:text-gray-300"
            />
          </div>

          <div>
            <label class="block text-[12px] font-bold uppercase tracking-wider text-gray-500 mb-1 ml-1">Wifi Available</label>
            <select name="wifi" required
              class="w-full border border-gray-300 rounded-xl px-4 py-3 focus:outline-none focus:ring-2 focus:ring-black transition-all bg-white text-sm">
              <option value="" disabled selected>Select option</option>
              <option value="yes">Yes</option>
              <option value="no">No</option>
            </select>
          </div>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
          <div>
            <label class="block text-[12px] font-bold uppercase tracking-wider text-gray-500 mb-1 ml-1">Parking Available</label>
            <select name="parking" required
              class="w-full border border-gray-300 rounded-xl px-4 py-3 focus:outline-none focus:ring-2 focus:ring-black transition-all bg-white text-sm">
              <option value="" disabled selected>Select option</option>
              <option value="yes">Yes</option>
              <option value="no">No</option>
            </select>
          </div>

          <div>
            <label class="block text-[12px] font-bold uppercase tracking-wider text-gray-500 mb-1 ml-1">Air Conditioning</label>
            <select name="ac" required
              class="w-full border border-gray-300 rounded-xl px-4 py-3 focus:outline-none focus:ring-2 focus:ring-black transition-all bg-white text-sm">
              <option value="" disabled selected>Select option</option>
              <option value="yes">Yes</option>
              <option value="no">No</option>
            </select>
          </div>
        </div>

        <div>
          <!-- //form here im trying to get image as file and to cloudinary -->
          <label class="block text-[12px] font-bold uppercase tracking-wider text-gray-500 mb-1 ml-1">Image/Video</label>
          <div class="relative">
            <input
              type="file"
              multiple
              name="images"
              placeholder="image/video of House"
              required
              class="w-full border border-gray-300 rounded-xl px-4 py-3 focus:outline-none focus:ring-2 focus:ring-black transition-all placeholder:text-gray-300"
            />
            <div class="absolute inset-y-0 right-4 flex items-center pointer-events-none">
              <svg class="w-5 h-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
              </svg>
            </div>
          </div>
        </div>

        <div>
          <label class="block text-[12px] font-bold uppercase tracking-wider text-gray-500 mb-1 ml-1">Description</label>
          <textarea
            rows="5"
            placeholder="Share what makes your place special..."
            name="description"
            required
            class="w-full border border-gray-300 rounded-xl px-4 py-3 focus:outline-none focus:ring-2 focus:ring-black transition-all placeholder:text-gray-300 resize-none"
          ></textarea>
        </div>

        <div class="pt-4">
          <button
            type="submit"
            class="w-full bg-gradient-to-r from-[#FF385C] to-[#E31C5F] text-white py-4 rounded-xl font-semibold text-lg hover:shadow-lg active:scale-[0.98] transition-all"
          >
            Create Listing
          </button>
        </div>

      </form>
    </div>

    <p class="text-center text-gray-400 text-sm mt-8">
      By clicking "Create Listing", you agree to our host terms and conditions.
    </p>

  </div>

</body>
</html>