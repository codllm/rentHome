<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Something went wrong · StayNest</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;800&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; }
        .error-ghost {
            animation: float 3s ease-in-out infinite;
        }
        @keyframes float {
            0% { transform: translateY(0px); }
            50% { transform: translateY(-20px); }
            100% { transform: translateY(0px); }
        }
    </style>
</head>
<body class="bg-white text-[#222222] antialiased">

    <nav class="h-20 px-6 md:px-12 flex items-center border-b border-gray-100">
        <a href="/" class="flex items-center gap-1 text-[#FF385C]">
            <svg viewBox="0 0 32 32" class="w-8 h-8 fill-current"><path d="M16 1L1 12h3v17h8v-9h8v9h8V12h3L16 1z"/></svg>
            <span class="text-xl font-bold tracking-tighter">stayNest</span>
        </a>
    </nav>

    <main class="max-w-[1200px] mx-auto px-6 py-20 flex flex-col md:flex-row items-center justify-between gap-12">
        
        <div class="flex-1 text-center md:text-left space-y-6">
            <h1 class="text-7xl md:text-9xl font-extrabold text-gray-100 absolute -z-10 opacity-50">
                <%= error.status || 'Error' %>
            </h1>
            
            <h2 class="text-4xl md:text-5xl font-extrabold text-gray-900 leading-tight">
                <%= message || "We can't seem to find the page you're looking for." %>
            </h2>
            
            <p class="text-lg text-gray-500 font-light max-w-md mx-auto md:mx-0">
                <% if (error.status === 404) { %>
                    It looks like the link is broken or the property has been removed.
                <% } else if (error.status === 500) { %>
                    Our servers are having a little trouble right now. Please try again in a moment.
                <% } else { %>
                    An unexpected error occurred. Don't worry, our team has been notified.
                <% } %>
            </p>

            <div class="flex flex-col sm:flex-row gap-4 pt-4 justify-center md:justify-start">
                <a href="/" class="px-8 py-3 bg-[#222222] text-white font-bold rounded-xl hover:bg-black transition-all active:scale-95 text-center">
                    Back to Home
                </a>
                <a href="/help" class="px-8 py-3 bg-white border border-gray-300 text-gray-700 font-bold rounded-xl hover:bg-gray-50 transition-all text-center">
                    Visit Help Center
                </a>
            </div>

            <% if (process.env.NODE_ENV === 'development') { %>
                <div class="mt-12 p-4 bg-red-50 border border-red-100 rounded-xl text-left">
                    <p class="text-xs font-mono text-red-600 truncate"><%= error.stack %></p>
                </div>
            <% } %>
        </div>

        <div class="flex-1 flex justify-center items-center">
            <div class="error-ghost relative">
                <svg viewBox="0 0 24 24" class="w-64 h-64 text-gray-100" fill="none" stroke="currentColor" stroke-width="0.5">
                    <path d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" stroke-linecap="round" />
                </svg>
                <div class="absolute inset-0 flex items-center justify-center">
                    <svg viewBox="0 0 24 24" class="w-20 h-20 text-[#FF385C]" fill="none" stroke="currentColor" stroke-width="1.5">
                        <path d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" stroke-linecap="round" stroke-linejoin="round" />
                    </svg>
                </div>
            </div>
        </div>

    </main>

    <footer class="max-w-[1200px] mx-auto px-6 py-10 border-t border-gray-100">
        <div class="flex flex-wrap gap-6 text-sm text-gray-400 font-light">
            <span>© 2026 stayNest, Inc.</span>
            <a href="#" class="hover:underline">Privacy</a>
            <a href="#" class="hover:underline">Terms</a>
            <a href="#" class="hover:underline">Sitemap</a>
        </div>
    </footer>

</body>
</html>